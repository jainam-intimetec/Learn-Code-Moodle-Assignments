namespace BookSystem.Services
{
    public class BookFinder
    {
        public string GetLocation(Entities.Book book)
        {   
            // returns the position in the library
            // ie. shelf number & room number
        }
    }
}